# Documents — planning docs stay local
*.pdf
*.md
!README.md
# Explicit negations, per CLAUDE.md: a doc that needs to ship gets a `!` line
# rather than a loosened rule. These two are read by a judge, not by us.
!docs/DEMO_RUNBOOK.md
!docs/DEFINITION_OF_DONE.md

# Go
/bin/
*.exe
*.test
*.out
vendor/

# Node / Next.js
node_modules/
/web/.next/
/web/out/
/web/build/

# Environment & secrets
.env
.env.*
!.env.example

# Local infrastructure state
/deploy/data/
*.log

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
